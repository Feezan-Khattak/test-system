import { makeStyles } from "@material-ui/styles";

const MainDiv = makeStyles({
	mainDiv: {
		padding: "2rem",
		width: "90%",
		marginLeft: "auto",
		marginRight: "auto",
	},
});

export default MainDiv;
