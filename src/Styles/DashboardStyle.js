import { makeStyles } from "@material-ui/core";

const DashboardStyle = makeStyles({
	mainContainer: {
		width: "90%",
		margin: "4rem, auto",
	},
	menuContainer: {},
	bodyContainer: {},
});
export default DashboardStyle;
